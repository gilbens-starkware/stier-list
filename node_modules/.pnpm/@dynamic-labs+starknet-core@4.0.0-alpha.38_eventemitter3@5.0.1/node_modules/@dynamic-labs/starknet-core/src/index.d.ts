import { RpcProvider } from 'starknet';
import { GenericNetwork, NetworkConfigurationMap } from '@dynamic-labs/types';
export * from '@dynamic-labs/rpc-providers';
type IStarknetRpcProvider = {
    chainId: string;
    chainName: string;
    provider: RpcProvider;
};
declare module '@dynamic-labs/rpc-providers' {
    type StarknetRpcProvider = IStarknetRpcProvider;
    interface RpcProviderLookup {
        starknet: StarknetRpcProvider[];
    }
    interface IChainRpcProviders {
        getStarknetRpcProviderByChainId(config: NetworkConfigurationMap, chainId: string): StarknetRpcProvider | undefined;
        registerStarknetProviders(): void;
        createProviderFromConfig(networks: GenericNetwork[]): StarknetRpcProvider[];
    }
}
